set timeout=0
set default=0

menuentry "Jarvis OS" {
    # Force the bootloader to hand over a text-mode screen
    set gfxpayload=text
    multiboot /boot/kernel.elf
    # 'boot' is implicit in menuentries, but doesn't hurt to keep
    boot
}